import { FilterproductoPipe } from './filterproducto.pipe';

describe('FilterproductoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterproductoPipe();
    expect(pipe).toBeTruthy();
  });
});
