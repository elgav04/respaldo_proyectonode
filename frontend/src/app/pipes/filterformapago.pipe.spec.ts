import { FilterformapagoPipe } from './filterformapago.pipe';

describe('FilterformapagoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterformapagoPipe();
    expect(pipe).toBeTruthy();
  });
});
