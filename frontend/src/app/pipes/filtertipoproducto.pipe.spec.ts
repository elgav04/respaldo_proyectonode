import { FiltertipoproductoPipe } from './filtertipoproducto.pipe';

describe('FiltertipoproductoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertipoproductoPipe();
    expect(pipe).toBeTruthy();
  });
});
