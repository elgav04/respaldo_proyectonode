import { FilterclientesPipe } from './filterclientes.pipe';

describe('FilterclientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterclientesPipe();
    expect(pipe).toBeTruthy();
  });
});
