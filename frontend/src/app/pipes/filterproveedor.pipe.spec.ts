import { FilterproveedorPipe } from './filterproveedor.pipe';

describe('FilterproveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterproveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
