import { FiltersucursalesPipe } from './filtersucursales.pipe';

describe('FiltersucursalesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersucursalesPipe();
    expect(pipe).toBeTruthy();
  });
});
