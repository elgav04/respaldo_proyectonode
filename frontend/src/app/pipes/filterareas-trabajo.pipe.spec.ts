import { FilterareasTrabajoPipe } from './filterareas-trabajo.pipe';

describe('FilterareasTrabajoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterareasTrabajoPipe();
    expect(pipe).toBeTruthy();
  });
});
